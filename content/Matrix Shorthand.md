to express an expression that affects all cells in a matrix, it can instead be expressed generally in terms of $i,j$ where:

- $i$ is the row in the matrix and
- $j$ is the column in the matrix

## Example
